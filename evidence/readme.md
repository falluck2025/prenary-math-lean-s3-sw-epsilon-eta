# evidence/ · 验证取证

本目录存放验证过程的截图（**每件一组，两张**）：

| 文件名 | 内容 |
|:--|:--|
| `DayXX-NN_内核.png` | 内核通过：`All Messages (0)`（光标置于文件末尾） |
| `DayXX-NN_Comparator.png` | Comparator 二次验证通过：`Trusting challenge with hash <SHA256>` ＋ 结果为通过 |

**纪律**：
1. **截图中的哈希须与最终落盘 SHA256 一致**；
2. **若中途重跑导致哈希变更，截图须重拍**（否则证据与文件不匹配）；
3. 平台：L∃∀N Comparator Live (Experimental) · Latest Mathlib with Lean v4.35.0。
